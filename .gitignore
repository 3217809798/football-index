# ---- 绝不能进仓库的凭据 ----
# publish.json 里含 FTP 账号密码；CI 里由 GitHub Secrets 现场生成
publish.json
# bifaw_conf.json 里含必发指数网账号密码；CI 里用 Secrets 注入环境变量
bifaw_conf.json
# baidu_push.json 里含百度主动推送 token；CI 里用 Secrets 注入环境变量
baidu_push.json
# _gh_token.txt 是 GitHub 的 Personal Access Token（用 _gh_push.py 推文件时用）
_gh_token.txt

# ---- 浏览器会话（含 cookie，体积大）----
bifaw_profile/

# ---- 运行时产物，CI 每次都会重新生成 ----
.publish-fingerprint
.publish-state
.publish.lock
logs/
dist/
publish.log
*.bak
__pycache__/
*.pyc

# ---- 本地工具目录 ----
.workbuddy/
.chrome-profile/
.venv/
ref/

# ---- 要提交的文件（备忘，无需取消注释）----
# index.html         页面本体，build_dist 需要它存在
# bf.json         竞彩必发数据快照（当前唯一数据源）
# bf-inline.js    file:// 打开时的内联兜底
# bifaw_fetch.py     抓竞彩必发（登录 + 验证码 OCR）
# publish.py         抓取+生成+上传
# build_publish_conf.py   CI 里由 Secrets 生成 publish.json
# .github/workflows/spdex-update.yml   主定时任务
# .github/workflows/keepalive.yml      保活任务（可选）
# GITHUB-ACTIONS-SETUP.md              操作说明
# TROUBLESHOOTING.txt                  排障说明
#
# 以下是已下线源（超级指数）的遗留物，提交与否都不影响流程：
# data.json / data-inline.js / spdex_fetch.py
